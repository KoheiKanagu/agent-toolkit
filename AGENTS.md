# エージェント共通ルール

各ルールは「**トリガー**(いつ)→ 行動(何をする)→ 理由(1文)」の形。トリガーに合致する作業を始める前に、該当ルールを適用する。

## このファイルの書き方(追記する人へ)

- 書いてよいのは「モデルのデフォルト動作からの差分」だけ: チームの決定、判断基準、失敗の補正(gotcha)。一般的なツールの使い方・手順は書かない — モデルは既に知っており、毎ターンのコンテキスト消費と陳腐化だけが残る。
- 1項目 = 1ルール。トリガーで書き始め、理由は1文まで。仕組みの解説(how)は書かない。チェックリストとして一体で完了すべきものだけ、同一項目内に番号付き条件を置いてよい。
- **追記する前に**: 既存項目を検索し、同じテーマがあれば新項目でなく既存項目を更新する。このファイルは毎ターン全文ロードされるので、重複は恒常的に二重課金される。

## コミットと push

以下の `origin/main` はリモートのデフォルトブランチを指す(名前が異なるプロジェクトでは読み替える)。

- 1変更 = 1論理コミット。
- **worktree・作業ブランチを作るとき**: 起点は必ず `origin/main`(リモート ref)。ローカルの `main` は未 push コミットを含むことがあり、PR の diff に混入する。
- **デフォルトブランチ上で既に変更・コミットしてしまったとき**: 現 HEAD から作業ブランチを切って退避し、ローカルのデフォルトブランチを origin に揃え直す。
- **worktree を作るとき**: 配置先は `../worktrees/<リポジトリ名>/<issue番号 or ブランチ名>` にする。リポジトリ外に置くことで、誤って本体の worktree や gitignore に混入するのを防ぐ。
- **push する前**(履歴書き換え(amend・rebase)をしたら、次の push の前に再確認): ベース汚染を確認する。`git fetch origin <base>` してから、次の両方を満たして完了とする — (1) `git diff --name-only origin/<base>...HEAD` にこの PR が触るはずのないパスがない、(2) `git cherry origin/<base> HEAD` に `-` で始まる行がない(ベースと patch 等価な重複コミット — 誤った rebase/reset がマージ済みの作業を再導入した印)。どちらかに該当したら rebase で取り除いてから push する。
- **stacked PR を rebase するとき**: 依存 PR がマージされたら、`git rebase --onto` の前に必ず `git fetch origin <base>`。古い base への rebase は依存のマージ済み変更を無言で落とす(rebase は自分の diff しか replay しない)。rebase 後、依存のシンボルが HEAD に残っていることを grep で確認して完了とする。
- **push するとき**: コミットを溜めて1回で push する。push ごとに CI とレビューが走るため。

## 長時間ジョブの監視

- **デプロイ・CI・データ移行などを起動したとき**: 終端状態(成功/失敗/タイムアウト)まで監視し、結果とエラー内容を報告して完了とする。「トリガーした」という報告で作業を終えない。
- **監視対象を選ぶとき**: 実際のジョブ機構(workflow run、パイプライン実行)を特定してその状態を見る。推測した下流の効果(デプロイ後のリソース状態など)を代わりに見ない — 別の原因でも変化し、誤判定する。
- **ポーリングループを書くとき**: break 条件に「終端状態」か「回数上限」を必ず含める。「特定の名前のチェックが現れたら」のような、永遠に満たされない可能性のある条件だけで待たない。
- **CI に赤いチェックを見つけたとき**: conclusion を確認してから対処する。`cancelled` は失敗ではない(連続イベントで concurrency group が古い run を止めた痕跡)。最新の run が成功していれば対処不要。

## GitHub 操作

- **PR body を更新するとき**: 最新の body を取得し、必要なセクションだけを差分編集する。他者・bot の編集や、人間が添付した画像・動画を上書きで消さない。
- **`#` + 数字を含むテキストを投稿するとき**: 裸の `#6` はその番号の PR/issue へ自動リンクされる。順序数は出力言語の序数表現(「6番」/ "No. 6")かインラインコード(`` `#6` ``)で書き、投稿前に裸の `#<数字>` の紛れ込みを確認する。
- **レビューの承認状況を判断するとき**: review の state(`APPROVED` / `CHANGES_REQUESTED` / `COMMENTED`)だけを根拠にする。本文の文言は根拠にしない(「approve 済み」と書かれた `COMMENTED` レビューが実在する)。レビュアーごとに最新の state が有効で、新しいコミットが push されたら古い `APPROVED` は stale。
- **マージ済みかを判断するとき**: fetch 済みの `origin/main`(`git grep <symbol> origin/main` 等)と issue tracker / PR の state を根拠にする。ドキュメント内の status 表記とローカルの main checkout は stale になり得るので根拠にしない。

## テストとコマンド実行

- **自分の変更後にテストが失敗したとき**: 原因は自分の変更として扱う(main は green が前提)。setup・ビルド順・生成ファイルの問題に見えても「既存の問題」と結論しない。flake を疑うときは1回だけ再実行し、それでも失敗するなら flake ではない。
- **pass/fail を判定するコマンド(ビルド・テスト)を実行するとき**: パイプでフィルタに繋がず、素で実行する。
  - ❌ `make test | tail -20` — exit code が tail のもの(常に 0)になり、失敗が成功と報告される
  - ✅ `make test` を素で実行し、出力が長すぎたときだけフィルタを検討する

## コードコメント

- **コメントを書く前のテスト**: 「次の読者は識別子と周辺コードから読み取れるか?」— Yes なら書かない(WHY であっても)。書くに値するのは、識別子が伝えられない非自明な制約・回避策・驚きだけ。
- **書くとき**: 1行で簡潔に。経緯・チケット参照・詳しい理由はコミットメッセージと PR 本文へ(そちらは詳細でよい)。
- 書かないパターン(いずれも実際に削除を求められた実例):
  - ❌ 将来の予告 — 「アラート送信は TICKET-123 で実装予定」
  - ❌ 使用箇所の説明 — 「この値は X.y に保存される」
  - ❌ 既存慣習との比較 — 「Entitlement と同じ規約」
  - ❌ ファイル外の文脈に依存する WHY — フェーズ名や未実装コンポーネントへの言及(単体で読めない)

## コンテキスト管理(サブエージェントを持つエージェント)

- **手を動かす粒度の作業が発生したとき**(サブエージェント機構がある環境のみ): 次のいずれかに該当したら委任する — 3ファイル超の変更、調査が2ホップ以上、テスト実行やログ精査を含む。該当しない自明な1ファイル修正はメインでよい。迷ったら委任側に倒す — メインのコンテキストは最も希少なリソース。
- **サブエージェントに委任するとき**: 対象パス(worktree)、使うべきツール・ラッパー、報告してほしい形式をプロンプトに明示する。サブエージェントはルールも会話文脈も継承しない。

## フォローアップ管理

- **作業を「先送りする」と決めたとき**: その場でフォローアップ issue を作り、コード内にリンクを残して完了とする。issue のない先送りは静かに消失するのと同じ。
- **期日のあるフォローアップ(期限日に設定を戻す等)を作るとき**: 期日付き issue に加えて能動的なトリガー(スケジュール実行・リマインダー)を必ず対にし、対象コードに期日と issue 番号のコメントを残す。issue だけでは当日誰も見ない。トリガーの指示文は文脈ゼロで実行できる自己完結の内容にする。

## 問題設定の扱い

- **問いが曖昧なとき、または提示された選択肢の枠では文脈上の事実(ログ・エラー・制約)を説明できない二者択一のとき**: 表面の問いに従う前に、文脈から「本当に解くべき問い」を1つ再定義して明示し、その枠で答える。再定義は答え方の枠の変更までとし、実装・仕様・依頼スコープの変更や別作業への着手はしない(必要ならフォローアップ issue かユーザー確認)。同一アプローチの反復失敗は「ガードレール」の3回ルールに任せ、この項目では停滞ターン数を判定しない。理由: モデルは目の前の質問に従い問題設定の誤りを固定し、かつ再定義を別タスク実行の許可と誤読しやすい。
- **依頼が単一で明確なとき**(例: 「PR 説明文を直して」): 再定義ルールは発動しない。依頼どおり遂行する。作業中に別の問題(例: テスト不足)に気づいても本題をすり替えず、その場で直さない — 言及・報告・フォローアップ issue までに留め、「ガードレール」の依頼外ルールが優先する。理由: 明確な依頼に再定義を重ねるとスコープ拡大になり、再定義と依頼外禁止が衝突して解釈が割れる。

## ガードレール(手段の暴走防止)

- **テスト・lint・型チェックが通らないとき**: 実装を直す。チェックの弱体化(assert 削除、テストの skip、期待値の書き換え、lint ルール無効化、型エラーの ignore)で green にしない — チェックは要求仕様の一部。仕様自体が変わったためにテストを変えるときは、その変更を明示して報告する。直せないなら「直せない」と報告する。
- **pre-commit フックが失敗したとき**: 直してから commit する。`--no-verify` で飛ばさない — フックはローカルの CI で、飛ばしても失敗が CI に移るだけ。
- **破壊的操作(`reset --hard`・force push・ブランチ/ファイル/データの削除・環境の作り直し)をしたくなったとき**: 「とりあえずきれいにする」ためには使わない。実行前に次の両方を満たして完了とする — (1) 何が失われるか(未 push コミット、他人の作業)を列挙した、(2) その操作が症状でなく原因に対応している — 既知の失敗にパターンマッチした症状でも、原因は別のことがある。
- **同じアプローチで3回失敗したとき、または方向性が定まらないとき**: 続けない。`skills/consult-advisor` があれば呼び出して別モデルからの視点を得てから次のアプローチを決める。未導入、またはアドバイスを受けても方向性が定まらない場合は、状況と試したことをまとめてユーザーにエスカレーションする。アドバイサーは意思決定せず助言だけ — 最終判断と実行は自分が行う。
- **依頼されていない問題に気づいたとき**: その場で直さない(勝手なスコープ拡大は diff とレビューを汚す)。フォローアップ issue にして報告する(「フォローアップ管理」)。明確な依頼の最中に別問題へ本題をすり替える再定義はしない(「問題設定の扱い」)。
- **結果を報告するとき**: 失敗は失敗と、スキップはスキップと書く。「たぶん動く」を「動作確認済み」と書かない。試していないことを完了と報告するのは、後工程での発覚をより高コストにするだけ。
- **不可逆・外部公開の操作(削除、force push、外部サービスへの投稿・送信、本番環境の変更)**: 明示の指示があるときだけ実行する。過去に承認があっても文脈が変わっていたら再確認する。

## 実行環境(sandbox)

- **シェルやツールが `Operation not permitted` / sandbox 由来の書き込み拒否で失敗したとき**、または **本題の完了に作業ディレクトリ・エージェント状態ディレクトリ・temp 以外への書き込みが必要で、迂回が2手以上・別バイナリ直叩き・手順の書き換えになるとき**: 本題を無理に迂回で進め切る前にユーザーへ報告する。報告は (1) 拒否されたパスまたは操作 (2) それが OS レベルの sandbox（実行隔離）の write 境界に当たると判断した根拠 (3) 快適に進めるなら隔離設定への書き込み許可パス追加やプロファイル変更が候補であること — を含め、設定変更の実行はユーザー判断を待つ。理由: sandbox 制約はユーザー側でしか緩められず、エージェントの静かな迂回は手間が見えず同じ詰まりを繰り返す。
  - ❌ 失敗をツール不具合として別実装に逃げ、sandbox に触れない
  - ✅ 「FVM の `bin/cache` への stamp 書き込みが拒否された。sandbox の書き込み許可に FVM パスを足すと Flutter ラッパー `dart` がそのまま使える」と報告して待つ

## ユーザーへの返答

- **ユーザーの発話が提案・疑問形(「XXの方がよくない?」「YYにしたら?」等)で、変更・方針の示唆を含むとき**: 実装・修正・設定変更に着手せず(進行中タスクでも提案に関わる変更は採否まで進めない)、まず賛否と理由を短く述べ、進める方針の合意を取ってから手を動かす。反対ならユーザー案で進めるか現行案を続けるかを1トピックで確認する。事実確認・説明だけの疑問(「この関数は純粋関数では?」等)は対象外。本項に当たる発話では、低リスクデフォルトがあっても本項が優先する。明確な指示(「XXに直して」「採用で」「その方針で進めて」)や、既に合意済み方針の続行、進行中タスクの直接原因で修正方針が一意に近いバグ修正は合意待ちなしで進めてよい。迷ったら合意を取る。理由: 提案を即実装指示と解釈すると、検討のための発言がスコープ変更になり、ユーザーが求めていない変更が進む。
  - ❌ 「XXの方がよくない?」→ すぐ XX に直す
  - ✅ 「XXの方がよくない?」→ 「賛成。理由は…。この方針で直しますか?」と止め、合意を待つ
  - ✅ 「この関数は純粋関数では?」→ 説明だけ返し、実装に着手しない
- **ユーザーに意思決定(方針・仕様・優先度など)を求めるとき**: 1返答につき確認は意思決定トピック1つだけ出し、その答えを待ってから次の確認や実装に進む。聞く1問は「いまの次の一手を最もブロックするトピック」にする。後続は「あとN点(例: DB選定)」のように件数と論点名まで予告してよいが、選択肢や質問文は出さない。コード・文脈・既存慣習で一意に決まること、または低リスクで妥当なデフォルトがあることは聞かず推奨を明示して進める。デフォルト採用か質問か迷ったら質問する。ユーザーが「まとめて聞いて」等で一括確認を明示した場合だけ、複数トピックを1返答にまとめてよい。理由: 複数問いの同時提示はユーザーの認知負荷を上げ、依存する決定では前の答えで後続が無効になる。
  - ❌ 「確認3点: (1) DBは? (2) 認証は? (3) ログは?」
  - ✅ 「先に1点だけ確認します。認証はセッションCookieとBearerのどちらにしますか?(あと2点: DB選定とログ形式)」
- **ユーザーへの返答で略称を使うとき**: その返答内の初出は正式名称にする。同一返答内で既に出したものだけ略してよい。直前の会話やユーザー側の略称使用を理由に初出から略さない — 返答を単体で読むと何の略かが伝わらない。分野で定着した頭字語(HTTP, JSON, PR, CI 等)は対象外。
  - ❌ 初出から「TF に上げて ASC で確認」
  - ✅ 「TestFlight に上げて App Store Connect で確認」(同一返答の2回目以降は TF / ASC 可)

## 学習ループ

- **PR を ready にする直前、またはレビュー完了直後**: セッション中に得た再利用可能なナレッジ(規約、gotcha、修正された誤り)をメモリに記録する。メモリの記録場所は導入先の AGENTS.md で定義する(未定義ならユーザーに確認する)。定期的な昇格の手順と判断基準は `skills/promote-learnings`。
